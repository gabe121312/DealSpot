# DealSpot uses only the Python standard library (http.server, urllib, xml, json).
# No third-party packages are required. This file exists so deployment platforms
# (Render, Railway, etc.) detect the Python environment correctly.
